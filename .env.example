NODE_ENV=production
PORT=3000
APP_ORIGIN=https://misticaura.com.au
PUBLIC_APP_URL=https://misticaura.com.au

DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_SSL=false

SESSION_DAYS=14
CSRF_COOKIE_NAME=mistic_csrf
SESSION_COOKIE_NAME=mistic_session

# 32-byte key, base64 encoded. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_ENCRYPTION_KEY=

SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Mistic Aura <hello@misticaura.com.au>"
SUPPORT_EMAIL=hello@misticaura.com.au

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=aud
